# Local dev: point at the main app's docker-compose `db` service.
# Production: an SSH-tunneled connection to the RDS instance via a dedicated, tightly-scoped
# `chat_trends_worker` Postgres role (SELECT on "ChatMessages"/"ChatChannels" only, plus CREATE on
# schema public for this worker's own tables) — see DEPLOYMENT-CHAT-TRENDS-WORKER.md.
DATABASE_URL=postgres://enkl:changeme@localhost:5432/enkl
